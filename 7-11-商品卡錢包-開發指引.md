# 7-ELEVEN 商品卡本機錢包 — 開發指引

> 本文件供 Claude Code 直接讀取作為開發依據。
> 版本：v1.3（2026-09-14）｜ 目標平台：PWA（iOS Safari / Android Chrome）
> 各版變更與決策理由見 §13。

---

## 0. 專案一句話

把散落的實體商品卡條碼集中到手機裡，結帳時自動算出該刷哪幾張，並在刷完後回填餘額。

**明確不做的事：** 不查詢 7-11 線上餘額（無公開 API，官方存卡功能已於 2024/01 終止）、不上傳雲端、不做多人共享、不做原生 App。

**使用情境前提：** 手上卡片以 35 / 50 元小面額為主，張數多。演算法與預設值以此為準（§8、§4 Settings）。

**實體卡照樣保留。** App 不取代實體卡：卡片毀損但條碼與卡號仍可辨識時可申請補發（§1），丟掉實體卡就失去這條路。

---

## 1. 卡片事實依據

以下來自實體卡面文字與 T-01 解碼結果（2026-09-13，35 元卡），是所有設計的依據，不得推翻：

| 事實 | 對設計的影響 |
| --- | --- |
| 卡面有**兩條一維條碼**，上下排列 | 出示頁一張卡要能同時顯示兩條（FR-04） |
| 上方條碼：Code 128（`]C0`，非 GS1），內容 = 下方印的 16 碼數字（範例 `7100xxxxxxxxxxxx`，實卡號不進版控） | 繪製用 Code 128；此為 `Card.code` |
| 下方條碼：Code 128（`]C0`），內容為 **8 位英數字**，卡面**沒有印出** | 隱藏碼無法人工抄寫，只能掃描或從照片解碼取得；此為 `Card.hiddenCode`，推定為真正的驗證憑證 |
| QR Code 內容為靜態網址 `http://www.7-11.com.tw/service/card.asp`（旁註「掃描QR 了解商品卡」） | 忽略 QR，不存、不繪 |
| 卡面無刮刮碼 | 除上述兩條條碼外無其他憑證 |
| 「本卡無使用期限」 | 資料模型不需要到期日邏輯 |
| 每張卡印有發行日與履約保證期間（例：2026/07/01～2027/09/30） | 保證期間屆滿不影響使用；**不記錄**（使用者決策） |
| 「可分次及併卡使用，無法重複儲值」 | 多卡湊金額演算法成立；不需做加值功能 |
| 「本卡屬無記名，若已拆封、使用、遺失、被竊或毀損致條碼及卡號無法辨識，恕無法退現、掛失止付或補發；如人為毀損但條碼及卡號仍可辨識者，得申請補發，每次費用 50 元」 | 條碼 = 不記名現金，威脅模型見 §6.2；實體卡應保留以備補發 |
| 「本卡為紙卡製作，請妥善保管」 | 紙卡易磨損，照片備援（FR-01）更有必要 |
| 「本卡可透過門市、交易明細、客服中心查詢餘額」 | 餘額唯一可信來源是**零值交易明細（收據）**，回填流程圍繞它設計 |
| 「至門市兌換商品時開立零值交易明細」 | 每次消費必有一張明細可讀餘額 |
| 恕無法兌換：菸品、代收代售、遊戲點數卡、宅急便、電信儲值卡、i預購、線上購物中心等 | UI 需有靜態提醒，不做自動偵測 |

---

## 2. 前置任務（寫任何 UI 之前先做完）

這三項都用電腦上的 Python 完成，**不需要先寫 App**。套件：`py -m pip install zxing-cpp pillow python-barcode`（Python 3.14 可安裝，已驗證）。

### T-01 判定條碼 symbology 與內容 ✅ 已完成

```
py tools\t01_detect.py samples\card_front.jpg
```

用**整張卡片正面照片**（不要裁切）。結果已寫入 §1：兩條 Code 128（`]C0`）＋一個靜態網址 QR。

之後拿到不同面額（50 / 100 元）的卡時，各跑一次確認格式相同；識別碼若出現 `]C1`（GS1-128）或內容長度不同，停下來確認。

### T-02 判定 QR Code 用途 ✅ 已完成

QR 為靜態介紹網址 → **忽略 QR**。

### T-03 現場驗證（不可跳過）

1. 重繪兩條條碼並回讀驗證：

```
py tools\t03_redraw.py samples\card_front.jpg
```

輸出 `samples\redraw_card.png`（兩條上下排列，仿卡面），腳本會確認重繪圖解出的內容與順序與原卡完全一致。

2. 把 `redraw_card.png` 傳到手機，全螢幕、亮度手動調到最高，拿**已知餘額**的卡到門市實刷一次小額商品（例如一瓶飲料）。
3. **記錄店員怎麼掃**：只掃上面、只掃下面、還是兩條都掃；掃的順序。結果寫入 §12，決定 FR-04 的出示版面。

- 成功 → 進入正式開發。
- 失敗 → 先確認是哪一條沒讀到；條碼路線作廢時改走 §7「降級方案」。

> 重繪圖等同現金：不要傳給別人、不要上傳雲端相簿、測完即刪除手機上的副本。

> ~~T-03 未通過前，不要投入超過 1 天的開發時間。~~
> **v1.3 決策（2026-09-14，使用者）：T-03 暫緩，先開發到 M2 部署上手機，再用 App 本身的出示頁做 FT-01 門市實刷。**
> 風險：若實刷失敗，出示頁需改走 §7 降級方案；M1 演算法與資料層不受影響。

---

## 3. 技術選型

| 項目 | 選擇 | 理由 |
| --- | --- | --- |
| 形態 | PWA，加入主畫面 | 免上架、iOS/Android 通吃、離線可用 |
| 框架 | React + Vite + TypeScript | 生態成熟、Claude Code 熟悉；維護者主力為 Python，程式需註解清楚 |
| 儲存 | IndexedDB（透過 `idb` 套件） | 照片以 **Blob** 直接存，不轉 base64（省 33% 空間） |
| 掃描 | 原生 `BarcodeDetector`，不支援時用 `barcode-detector` polyfill（內部為 zxing-wasm） | **iOS Safari 沒有原生 BarcodeDetector**，iPhone 上 polyfill 就是主路徑 |
| 繪製 | `JsBarcode`，格式 `CODE128`（自動選字元集） | T-01 確認兩條皆為一般 Code 128；QR 不需繪製，不引入 `qrcode` |
| 加密 | Web Crypto API：PBKDF2-SHA256 + AES-GCM | 僅用於匯出檔與 PIN 雜湊，不引第三方加密庫 |
| 測試 | Vitest（單元）+ Playwright（流程） | 演算法必須有單元測試 |
| 部署 | GitHub Pages | 見 §3.1 |

**不要引入：** 任何後端、任何分析追蹤 SDK、任何雲端同步、任何 CDN 載入的資源。

**掃描套件注意：** zxing-wasm 預設從 jsDelivr 下載 `.wasm`，違反 NFR-01。必須用 polyfill 提供的設定（`prepareZXingModule` / `locateFile`）改為從本站路徑載入，並把 wasm 檔納入建置輸出與 Service Worker 快取。

**照片規格：** 拍攝後於前端壓縮為長邊 1600px、JPEG 品質 0.8，再存入 IndexedDB。

### 3.1 部署與網址

- 部署於 GitHub Pages：`https://<帳號>.github.io/<repo>/`，Vite `base` 設為 `/<repo>/`。
- **網址自上線第一天起固定。** IndexedDB 資料綁定 origin（`<帳號>.github.io`）；改 GitHub 帳號名稱 = 換 origin = 手機上資料全部消失。改 repo 名稱會讓 Service Worker 與主畫面捷徑失效。
- 同一 GitHub 帳號下所有 Pages 專案共用同一個 origin，儲存空間彼此可見。IndexedDB 資料庫名稱使用專屬前綴（如 `seven-card-wallet`），且不在同帳號 Pages 放第三方腳本。
- 免費方案的 Pages 需公開 repo。原始碼不含個資，但**真實卡片照片、測試用實卡號一律不得進版控**：`samples/`、`*.jpg`、`*.png`（`public/icons/` 例外）寫入 `.gitignore`；單元測試使用虛構卡號。
- GitHub Pages 無法設定 HTTP header，以 `<meta http-equiv="Content-Security-Policy">` 限制 `connect-src 'self'`、`script-src 'self' 'wasm-unsafe-eval'`。
- 手機實機開發：`vite --host` 搭配 `@vitejs/plugin-basic-ssl`（相機必須在 HTTPS 下才能開）。

---

## 4. 資料模型

```ts
type BarcodeFormat = 'CODE_128';
// T-01 確認兩條皆為 Code 128；保留型別以便日後其他面額格式不同時擴充
// 掃描套件回傳 'code_128'，需經對應函式轉換

interface Card {
  id: string;                 // uuid
  code: string;               // 上方條碼內容（16 位數字，卡面有印）；畫面上只顯示末 4 碼
  hiddenCode: string;         // 下方條碼內容（8 位英數字，卡面未印）；畫面上一律不顯示文字
  format: BarcodeFormat;      // 掃描時寫入
  faceValue: number;          // 面額，整數，例如 35
  balance: number;            // 目前餘額，整數（使用者維護）
  balanceSource: 'receipt' | 'estimated' | 'initial';
  nickname?: string;          // 例如「客戶送的」
  photoBlob?: Blob;           // 原始卡片照片（已壓縮），掃不到時備援
  status: 'active' | 'archived';
  createdAt: number;          // 同餘額時的排序依據，確保演算法結果穩定
  updatedAt: number;
}

interface Transaction {
  id: string;
  status: 'pending' | 'confirmed';   // 出示條碼前即寫入 pending，回填確認後改 confirmed
  createdAt: number;
  confirmedAt?: number;
  totalAmount: number;        // 該筆結帳金額
  strategyUsed: 'min_cards' | 'clear_fragments' | 'manual';
  lines: Array<{
    cardId: string;
    order: number;            // 出示順序，從 1 起算
    plannedDeduct: number;    // 建議扣款
    balanceBefore: number;
    balanceAfter?: number;    // 使用者從收據回填；pending 時為空
    swiped: boolean;          // 出示頁「已刷過」勾選
  }>;
  cashTopUp: number;          // 卡不夠時另外付的現金，可為 0
  note?: string;
}

interface Settings {           // 存 IndexedDB 的 settings store，單一文件
  defaultStrategy: 'min_cards' | 'clear_fragments';   // 預設 'clear_fragments'
  maxCardsPerTransaction: number;   // 預設 6
  archiveThreshold: number;         // 餘額 <= 此值自動建議封存，預設 0
  lockEnabled: boolean;
  lockMethod: 'webauthn' | 'pin';
  pinHash?: string;                 // PBKDF2 結果（base64），不存明文 PIN
  pinSalt?: string;
  webauthnCredentialId?: string;
  failedAttempts: number;
  lockedUntil?: number;
  lastExportAt?: number;            // FR-07 備份提醒依據
  backupReminderSnoozedUntil?: number;
  onboardingDone: boolean;
}
```

**索引：** `Card.status`、`Card.balance`、`Card.code`（唯一）、`Card.hiddenCode`（唯一）、`Transaction.status`。

**格式驗證：** `code` 必須符合 `/^\d{16}$/`；`hiddenCode` 必須符合 `/^[0-9A-Z]{8}$/`（依目前樣本推定，出現不符的卡時停下來確認，不要放寬規則）。

**寫入原則：** 回填確認時，更新所有相關 `Card.balance` 與將 `Transaction.status` 改為 `confirmed`，必須在**同一個 IndexedDB transaction** 內完成。

---

## 5. 功能需求

### FR-01 新增卡片

- 相機掃描，**必須同時取得兩條條碼**才能存檔：以格式辨識（16 位數字 → `code`；8 位英數字 → `hiddenCode`），不依畫面上下位置判斷。只掃到一條時畫面提示「還差卡號條碼」或「還差下方條碼」並持續掃描。
- **必須同時拍一張卡片照片存入 `photoBlob`。** 這是現場掃不到時唯一的退路。
- 即時掃描失敗時，改為對剛拍的照片做離線解碼（同一套掃描套件）。
- **隱藏碼無法手動輸入**（卡面沒印）。照片也解不出時，提示重拍（正對、無反光、補光），不得只存卡號。
- 卡號可手動輸入僅作為「照片解碼成功但卡號條碼破損」的補救，並與卡面數字人工核對。
- 面額以快捷按鈕選擇（35 / 50 / 100 / 其他），`balance` 預設等於 `faceValue`，`balanceSource = 'initial'`；已用過部分的卡可直接修改餘額。
- 重複 `code` 或重複 `hiddenCode` 時阻擋新增並提示已存在（顯示既有卡片的暱稱與末 4 碼）；兩者只有一個重複時，另外提示「資料可能有誤，請重掃」。
- 支援連續新增模式：存完一張直接回到掃描畫面，因為小卡張數多。
- 非 standalone 模式下不得新增（見 §6.3）。

### FR-02 卡片清單

- 預設只顯示 `status = 'active'`，依餘額由小到大排序。
- 顯示：暱稱、**卡號末 4 碼**、餘額、餘額來源標記（估算值需視覺上明顯區隔）。
- 頂部顯示可用總額與張數。

### FR-03 結帳建議（核心）

輸入結帳金額（正整數）→ 輸出一份**有順序的刷卡清單**。演算法見 §8。

- 提供兩種策略切換，預設「優先清零頭」。
- 建議結果要顯示：每張刷多少、刷完後餘額、是否需補現金。
- 策略 B 若因避免多付現金而改用策略 A 的結果，顯示一行說明（`PlanResult.note`）。
- 使用者可手動增減卡片，系統依 §8.6 即時重算金額分配。

### FR-04 出示條碼

- 按下「開始出示」時，先寫入一筆 `status = 'pending'` 的 `Transaction`，再進入出示頁。
- 全螢幕白底顯示條碼，申請 **Screen Wake Lock** 防止螢幕熄滅，離開頁面時釋放。
- **每張卡同時顯示兩條條碼，仿卡面上下排列**：上方 `code`（條碼下印 16 碼數字）、下方 `hiddenCode`（不印文字），兩條間距 ≥ 條碼高度的 1/3，避免掃描器一次框到兩條。兩條都必須完整落在一個畫面內，不得需要捲動。
- 版面以常數集中定義（`BARCODE_LAYOUT`），T-03 / FT-01 若發現店員只掃其中一條或需要分開出示，只改此處。
- **網頁無法控制螢幕亮度**：進入出示頁時顯示一行不遮擋條碼的提示「請將亮度調到最高」，可設定為不再顯示。
- 多張卡時提供左右滑動切換，並標示「第 1 / 6 張」與該張卡號末 4 碼。
- 每張下方有「已刷過」勾選，避免店員刷到一半你搞混。
- 提供「顯示原始照片」按鈕（FR-01 存的 `photoBlob`）。
- 條碼以 canvas 依 `devicePixelRatio` 繪製，模組寬度取整數實體像素，左右留足靜區（≥ 10 倍模組寬）。
- 提供「這筆沒刷成功」按鈕：刪除該筆 pending 交易，餘額不變。

### FR-05 餘額回填

刷完後進入回填頁：

- 每張卡顯示卡號**末 4 碼**與**推算餘額**作為預設值，方便對照收據。
- 使用者對照收據（零值交易明細）確認或修正。
- 確認後 `balanceSource = 'receipt'`；直接略過則為 `'estimated'`。
- 確認後 `Transaction.status = 'confirmed'`，與卡片餘額在同一個 DB transaction 寫入。
- **App 啟動（含解鎖後）若存在 pending 交易，直接進入回填頁**，不得讓使用者在未處理前開始新的結帳。

### FR-06 封存（取代刪除）

- 餘額歸零時**不自動刪除**，改為提示封存。
- 封存卡片可在「已封存」頁查看與還原。
- 實體刪除必須經過二次確認，且明確警告不可復原。

> 理由：餘額是推算值，可能有誤差。刪錯就是把現金丟掉。

### FR-07 備份與還原

- 匯出：全部資料（含照片）打包成單一加密檔，副檔名 `.7cw`。
- 加密：使用者自設密碼 → PBKDF2-SHA256（**600,000 iterations**）→ AES-GCM 256。
- 檔案格式：`魔術字 "7CW1"`｜`格式版本 (1 byte)`｜`iterations (uint32)`｜`salt (16 bytes)`｜`iv (12 bytes)`｜`密文`。明文為 JSON：`{ schemaVersion, exportedAt, cards（照片轉 base64）, transactions, settings（排除 PIN 與 WebAuthn 欄位） }`。
- 匯出輸出方式：優先 `navigator.share({ files })`（iOS 可存到「檔案」或傳 LINE）；不支援時才用下載連結。iOS 主畫面模式下 `<a download>` 不可靠。
- 匯入：`<input type="file">` **不限制 accept**（iOS 會把未知副檔名顯示為灰色不可選）；輸入密碼解密，提供「合併」與「覆蓋」兩種模式。
- 設定密碼時明確警告：**忘記密碼，備份檔永遠無法解開。**
- **App 需在超過 14 天未匯出時主動提醒**，提醒不可關閉，只能延後（最多 3 天）。

### FR-08 應用鎖

- 啟動時要求 WebAuthn（平台驗證器，即 Face ID / 指紋）或 6 位數 PIN。
- PIN 以 PBKDF2 雜湊儲存；連續錯 5 次鎖定 1 分鐘，之後每次再錯鎖定時間加倍。
- 背景超過 2 分鐘回前景需重新驗證（以 `visibilitychange` 記錄時間戳判斷，不依賴背景計時器）。
- 重新驗證不得清除進行中的結帳狀態；解鎖後回到原畫面或 FR-05 回填頁。
- 應用鎖的防護範圍見 §6.2，UI 與文件不得宣稱「資料已加密」。

---

## 6. 非功能需求

| 編號 | 需求 |
| --- | --- |
| NFR-01 | 全離線運作。除本站靜態資源外不得發出任何網路請求（以 CSP `connect-src 'self'` 強制）。 |
| NFR-02 | 不得將卡號、隱藏碼、餘額、照片寫入 localStorage、URL 參數或 console。`hiddenCode` 在任何畫面都不以文字顯示。 |
| NFR-03 | 效能（不含使用者操作時間）：冷啟動到解鎖畫面可操作 ≤ 1.5 秒；解鎖到結帳鍵盤可輸入 ≤ 0.5 秒；按下「開始出示」到條碼顯示 ≤ 0.5 秒。 |
| NFR-04 | Service Worker 快取所有資源（含 zxing wasm），飛航模式下功能完整。 |
| NFR-05 | 匯出檔案不得含明文卡號（整檔加密即滿足）。 |
| NFR-06 | 支援深色模式，但條碼區一律強制白底黑條（掃描器對反相條碼辨識率低）。 |
| NFR-07 | 所有金額為整數（新台幣元），演算法與 DB 不得出現小數。 |

### 6.1 iOS Safari 資料遺失風險（必須處理）

Safari 對未加入主畫面的網站，7 天未使用可能清除 IndexedDB。因應措施：

1. 首次啟動強制引導「加入主畫面」（§6.3）。
2. 呼叫 `navigator.storage.persist()` 申請持久化配額。
3. FR-07 的備份提醒不可關閉，只能延後。

### 6.2 威脅模型（已決策：本機明文 + 應用鎖）

| 防護 | 手段 |
| --- | --- |
| 他人拿到已解鎖的手機亂點、肩窺 | 應用鎖、背景 2 分鐘重鎖 |
| 備份檔外流 | 匯出檔整檔 AES-GCM 加密 |
| 資料不小心外流到網路 | 無後端、CSP 禁止外部連線、NFR-02 |

**真正的秘密是 `hiddenCode`：** 卡號印在卡面上，隱藏碼只存在條碼裡。推定持有兩者才能消費（待 FT-01 確認），因此卡片清單、回填頁等一律只顯示卡號末 4 碼，隱藏碼只在出示頁以條碼形式出現。

**明確不防：** 他人取得手機並以開發者工具、越獄或惡意軟體直接讀取 IndexedDB。
**理由：** 本機加密的金鑰若由 6 位數 PIN 衍生，可被離線暴力破解，實益低；改用長密碼則每次結帳都要輸入，且忘記密碼即失去所有卡片，風險高於收益。
**使用者應知：** 手機遺失時卡片無法掛失，應盡快用另一台裝置還原備份並把卡用掉。

### 6.3 Standalone 模式限制

iOS 上 Safari 分頁與「加入主畫面」的 App **儲存空間互相獨立**，在分頁中存的卡，主畫面 App 看不到。

- 以 `matchMedia('(display-mode: standalone)')` 與 `navigator.standalone` 判斷。
- 非 standalone 時只顯示「加入主畫面」教學頁，**所有寫入功能停用**。
- 開發模式（`import.meta.env.DEV`）與 E2E 測試建置旗標（`VITE_ALLOW_BROWSER_MODE`）例外。

---

## 7. 降級方案（T-03 失敗時啟用）

若重繪條碼無法被 POS 讀取，保留以下價值：

- App 退化為「卡片餘額管理 + 刷卡順序建議」。
- 出示階段改為顯示 FR-01 存下的**原始卡片照片**（裁切至條碼區、旋轉校正、白底全螢幕、Wake Lock）。
- 這仍然比相簿好用：有順序建議、有餘額、不用翻幾百張照片。

---

## 8. 配卡演算法規格

### 8.1 前提

- 商品卡「可分次及併卡使用」，同一筆交易可刷多張。
- 實務上店員逐張刷，POS 扣到該卡餘額用盡再換下一張。
- 因此輸出必定是：**前 N-1 張全額用盡，最後一張部分使用（或剛好用盡）**。
- 以 `maxCardsPerTransaction`（預設 6）為自動建議的硬上限；手動調整可超過，但顯示警告。
- 所有計算為整數。純函式，不讀寫 DB，不拋例外。

### 8.2 輸入 / 輸出

```ts
interface PlanInput {
  amount: number;              // 結帳金額
  cards: Card[];               // 呼叫端傳入全部卡片；函式內自行過濾
  strategy: 'min_cards' | 'clear_fragments';
  maxCards: number;
}

interface PlanResult {
  lines: Array<{ card: Card; deduct: number; balanceAfter: number }>;  // 已依出示順序排列
  covered: number;             // 卡片共支付
  cashTopUp: number;           // 需補現金，0 表示全額由卡支付
  fullyConsumedCount: number;  // 本次會清空幾張
  error?: 'INVALID_AMOUNT';    // 金額非正整數時
  warning?: 'NO_CARDS' | 'NEED_CASH';
  note?: 'FALLBACK_TO_MIN_CARDS';  // 策略 B 改用策略 A 的結果時
}
```

### 8.3 共用前置與排序規則

```
1. amount 非正整數 → lines = []，covered = 0，cashTopUp = 0，error = 'INVALID_AMOUNT'。
2. usable = cards 中 status === 'active' 且 balance > 0 者。
3. usable 為空 → lines = []，cashTopUp = amount，warning = 'NO_CARDS'。
4. 「由小到大」「最大者」「最小者」遇同餘額時，依 createdAt 由舊到新，確保結果穩定。
5. 輸出順序：全額用盡的卡依 balance 由小到大，部分使用的卡（至多一張）放最後。
6. cashTopUp > 0 時 warning = 'NEED_CASH'。
```

### 8.4 策略 A：min_cards（最少張數）

```
remaining = amount；picked = []
while picked.length < maxCards 且 remaining > 0 且尚有未選的卡：
    cover = 未選卡中 balance >= remaining 的最小者
    若 cover 存在：選入 cover，remaining = 0，結束
    否則：選入未選卡中 balance 最大者，remaining -= 其 balance
cashTopUp = remaining
```

> 「能一張付清就挑剛好夠的最小卡，不能就先拿最大的」。張數最少，且在張數上限內卡片支付金額最大，因此**策略 A 的 cashTopUp 是所有可能組合中的最小值**。

### 8.5 策略 B：clear_fragments（優先清零頭，預設）

**B-1 貪婪候選 greedyB：**

```
remaining = amount；picked = []
1. fragments = usable 中 balance < amount 者，由小到大。
2. 從 fragments 依序選入，最多 (maxCards - 1) 張，
   每選一張 remaining -= balance；remaining <= 0 立即停止。
3. while remaining > 0 且 picked.length < maxCards 且尚有未選的卡：
       closing = 未選卡中 balance >= remaining 的最小者
       若存在：選入 closing，remaining = 0，結束
       否則：選入未選卡中 balance 最大者，remaining -= 其 balance
4. cashTopUp = max(remaining, 0)
```

**B-2 最終選擇：**

```
candidates = [greedyB, 策略 A 結果]
依序比較，取較佳者：
  (1) cashTopUp 較小
  (2) fullyConsumedCount 較大（清掉的零頭多）
  (3) lines 張數較少
  (4) 仍相同 → 取 greedyB
若選中策略 A 且 greedyB.cashTopUp > 策略 A.cashTopUp → note = 'FALLBACK_TO_MIN_CARDS'
```

> 這保證：**策略 B 永遠不會比策略 A 多付現金**；有一張剛好等於金額的卡、且清掉的張數不比貪婪結果少時，只刷那一張。

### 8.6 手動調整分配（FR-03）

```
allocate(selectedCards, amount):
  1. selectedCards 依 balance 由小到大排序（同餘額依 createdAt）。
  2. remaining = amount；依序 deduct = min(balance, remaining)，remaining -= deduct。
  3. deduct 為 0 的卡不列入 lines，另回傳 unusedCards 供 UI 提示「這張用不到」。
  4. cashTopUp = remaining。
  5. 張數超過 maxCards → 允許，但 UI 顯示警告。
```

策略 A、B 的輸出也必須能以 `allocate(選出的卡, amount)` 重算出相同的 deduct，作為一致性檢查。

### 8.7 設計取捨說明

> 不使用精確最佳化（背包 DP）。卡片數量少、使用者實際在乎的是「零頭別越積越多」而不是數學最優。貪婪 + 候選比較 + maxCards 上限的結果更可預期，也更好解釋給自己聽。

若日後 active 卡片數超過 50 張且建議結果明顯不佳，再考慮改 DP，並將此決策記入 ADR。

---

## 9. UI 方向

三個分頁：**卡片** / **結帳** / **設定**。不要做側邊抽屜。

- **結帳頁是主畫面。** 解鎖後直接落在數字鍵盤上，因為這是站在櫃檯前最急的動作（有 pending 交易時例外，先進回填頁）。
- 條碼出示頁全螢幕、白底、無任何裝飾、無動畫。這個畫面唯一的工作是被掃到。
- 餘額用等寬數字對齊，方便快速比對收據。
- 卡片一律以「暱稱＋末 4 碼」識別，與收據對照。
- 空狀態文案寫指令不寫情緒：「掃描第一張卡片的條碼」，不是「這裡還空空的」。
- 估算餘額與收據確認餘額必須一眼可分辨（例如估算值加虛線底線），因為它決定你敢不敢相信建議。
- 首次使用與結帳頁放置靜態提醒：菸品、代收代售、遊戲點數卡等不可使用（§1）。

---

## 10. 測試情境

### 10.1 演算法單元測試（Vitest，必做）

未註明時 `maxCards = 6`，卡片以餘額表示，測試用卡號一律虛構。

| ID | 情境 | 輸入 | 預期 |
| --- | --- | --- | --- |
| UT-01 | 單張剛好足夠 | amount=100，卡 [100] | 兩策略皆 1 張，deduct 100，balanceAfter 0，cashTopUp 0 |
| UT-02 | 單張略多 | amount=85，卡 [35, 100, 500] | 策略 A：100 扣 85；策略 B：35 全額 → 100 扣 50 |
| UT-03 | 零頭優先 | amount=200，卡 [12, 23, 500] | 策略 B：12 → 23 → 500 扣 165，清空兩張零頭 |
| UT-04 | 達 maxCards 上限 | amount=300，卡 [10,10,10,10,500]，maxCards=3 | 策略 B：10 → 10 → 500 扣 280；不得回傳 4 張 |
| UT-05 | 全部加總仍不足 | amount=1000，卡 [35, 50] | 兩策略皆 covered=85，cashTopUp=915，warning='NEED_CASH' |
| UT-06 | 無可用卡片 | amount=100，卡 [] | lines 為空，cashTopUp=100，warning='NO_CARDS'，不得拋例外 |
| UT-07 | 餘額為 0 或已封存的卡 | amount=50，卡 [0, 100(archived), 100] | 只出現 active 且 balance>0 的那張 |
| UT-08 | 金額不合法 | amount=0 / -50 / 12.5 | lines 為空，error='INVALID_AMOUNT'，不得產生負數扣款 |
| UT-09 | 單張剛好 vs 貪婪 | amount=50，卡 [30, 50] | 兩策略皆只刷 50（greedyB 30→50 扣 20 清空張數相同但多一張） |
| UT-10 | 大量零頭卡 | amount=500，20 張 5 元卡 + 1 張 1000 | 策略 B：5 張 5 元 → 1000 扣 475，共 6 張；不得回傳 20 張 |
| UT-11 | 扣款總和一致性 | 隨機，amount 為正整數 | `sum(deduct) + cashTopUp === amount` 恆成立 |
| UT-12 | 餘額不變性 | 隨機 | 任一 `0 <= balanceAfter <= balanceBefore`；除最後一張外 balanceAfter 皆為 0 |
| UT-13 | 策略 B 不多付現金 | amount=150，卡 [10, 20, 60, 70]，maxCards=3 | 策略 B 回傳 20 → 60 → 70（全額），cashTopUp 0，note='FALLBACK_TO_MIN_CARDS' |
| UT-14 | 零頭組合優於單張剛好 | amount=50，卡 [15, 35, 50] | 策略 B：15 → 35，保留 50；策略 A：只刷 50 |
| UT-15 | 典型小卡結帳 | amount=187，卡 [15, 35, 35, 50, 50, 50, 50] | 策略 B：15 → 35 → 35 → 50 → 50 → 50 扣 2，cashTopUp 0 |
| UT-16 | B 與 A 現金相同 | 隨機 | `B.cashTopUp === A.cashTopUp` 恆成立 |
| UT-17 | 結果穩定 | 同一組卡打亂輸入順序 | 輸出完全相同 |
| UT-18 | 手動分配 | allocate([100, 35], 40) | 35 全額 → 100 扣 5；順序與 deduct 正確 |
| UT-19 | 手動分配多餘卡 | allocate([35, 50, 500], 40) | 35 → 50 扣 5；500 列入 unusedCards |

> UT-11、UT-12、UT-16 用 property-based 隨機測試（`fast-check`）各跑 1000 組。

### 10.2 資料層測試

| ID | 情境 | 預期 |
| --- | --- | --- |
| DT-01 | 新增重複卡號或重複隱藏碼 | 阻擋並提示，DB 不得產生第二筆 |
| DT-01b | 只掃到一條條碼 | 不得存檔；不存在只有 `code` 沒有 `hiddenCode` 的卡 |
| DT-01c | 格式不符（隱藏碼 7 碼、含小寫、卡號含字母） | 阻擋並提示重掃 |
| DT-02 | 回填餘額後寫入 Transaction | Card.balance 與該筆 Transaction 的 balanceAfter 一致，status='confirmed' |
| DT-03 | 回填中途關閉 App | pending 交易保留、Card.balance 不變；重開 App 直接進回填頁 |
| DT-04 | 匯出後匯入（覆蓋） | 卡片數、餘額、照片完全一致 |
| DT-05 | 匯出後匯入（合併） | 相同 code 不重複建立，餘額以匯入檔為準並提示衝突 |
| DT-06 | 錯誤密碼匯入 | 明確報錯，不得留下部分寫入的資料 |
| DT-07 | 封存後還原 | 卡片回到 active 且餘額不變 |
| DT-08 | 「這筆沒刷成功」 | pending 交易刪除，所有卡片餘額不變 |
| DT-09 | 回填確認寫入中斷 | Card 與 Transaction 同時成功或同時失敗（單一 DB transaction） |
| DT-10 | 匯出檔內容 | 以二進位搜尋匯出檔，找不到任何一張卡的 16 碼卡號與 8 碼隱藏碼明文 |

### 10.3 流程測試（Playwright）

以 `VITE_ALLOW_BROWSER_MODE` 建置執行。

| ID | 情境 | 預期 |
| --- | --- | --- |
| E2E-01 | 首次啟動 | 引導加入主畫面 → 設定應用鎖 → 掃描第一張卡 |
| E2E-02 | 完整結帳 | 輸入 187 → 取得建議 → 逐張出示並勾選 → 回填餘額 → 清單更新 |
| E2E-03 | 手動調整建議 | 移除建議中的一張卡，金額分配即時重算且總額仍正確 |
| E2E-04 | 離線 | `context.setOffline(true)` 後全流程可用，無任何網路錯誤 |
| E2E-05 | 背景 3 分鐘後回前景 | 要求重新驗證，驗證後回到原畫面 |
| E2E-06 | 條碼頁 Wake Lock | 進入時申請、離開時釋放（mock `navigator.wakeLock` 驗證呼叫） |
| E2E-07 | 未完成回填後重開 | 直接進入回填頁，無法開始新結帳 |
| E2E-08 | 非 standalone 模式 | 不帶測試旗標建置時，新增卡片等寫入功能不可用 |
| E2E-09 | 網路請求稽核 | 全流程期間攔截所有 request，只有本站 origin |

### 10.4 現場實測（人工，無法自動化）

| ID | 情境 | 通過標準 |
| --- | --- | --- |
| FT-01 | 重繪條碼首次實刷（兩條上下排列） | 店員一次掃過，POS 正確扣款；記錄店員掃了哪幾條、順序 |
| FT-02 | 六張併刷 | 六張依序刷成功，收據餘額與 App 推算一致；記錄店員反應 |
| FT-03 | 螢幕貼防窺膜 / 低亮度環境 | 仍可掃描；否則需加大條碼尺寸 |
| FT-04 | 照片備援路徑 | 重繪條碼掃不到時，原始照片可被掃到 |
| FT-05 | 餘額不足補現金 | 流程順暢，店員無需額外操作 |
| FT-06 | iPhone 主畫面 App 掃描 | polyfill 掃描可在 3 秒內讀到卡片的兩條條碼 |
| FT-07 | 其他面額的卡 | 50 / 100 元卡跑 T-01，格式與長度與 35 元卡相同 |

---

## 11. 開發里程碑

| 階段 | 內容 | 完成標準 |
| --- | --- | --- |
| M0 | T-01 / T-02 / T-03 前置驗證（Python，不寫 App） | T-01、T-02 已完成；T-03 **暫緩**，併入 M2 後的 FT-01（v1.3） |
| M1 | 資料層 + 演算法 + 單元測試 | §10.1、§10.2 全綠 |
| M2 | 掃描、清單、出示條碼、部署 GitHub Pages | 手機主畫面 App 可存卡並在門市刷成功 |
| M3 | 結帳建議 + 餘額回填 + pending 交易 | E2E-02、E2E-07 通過 |
| M4 | 加密備份、應用鎖、PWA 離線 | §10.3 全綠 |
| M5 | 封存、設定、備份提醒 | 日常自用一個月無資料遺失 |

**M1 的演算法必須先於任何 UI 完成。** 它是整個工具的價值來源，UI 只是包裝。

**進度（2026-09-14）：**

| 階段 | 狀態 |
| --- | --- |
| M1 | ✅ UT-01～19、DT-01/01b/01c/02/03/07/08/09 全綠；DT-04～06、DT-10 屬匯出匯入，移到 M4 |
| M2 | ✅ 程式完成並部署；⏳ 待 iPhone 主畫面實機掃描（FT-06）與門市實刷（FT-01） |
| M3 | ✅ 程式完成；電腦預覽驗證 E2E-02、E2E-03、E2E-07、E2E-08 流程（手動，Playwright 未建） |
| M4、M5 | ⬜ 未開始（備份、應用鎖、備份提醒） |

---

## 12. 待確認清單

- [ ] 重繪條碼現場可讀性（T-03 / FT-01）
- [ ] 店員掃哪幾條、順序為何（T-03；決定 FR-04 `BARCODE_LAYOUT`）
- [ ] 隱藏碼是否為消費必要憑證（T-03 觀察；影響 §6.2 敘述）
- [ ] 店員是否接受一次刷 6 張（FT-02；不接受則調低 `maxCardsPerTransaction` 預設值）
- [ ] 零值交易明細上的餘額呈現格式：是否逐張列出、是否印卡號末 4 碼（影響回填 UI 的欄位對應）
- [ ] 50 / 100 元卡條碼格式是否與 35 元卡相同（FT-07）
- [x] 條碼 symbology（T-01）→ 兩條 Code 128 `]C0`：16 位數字卡號 ＋ 8 位英數字隱藏碼
- [x] QR Code 內容（T-02）→ 靜態網址，忽略
- [x] Python 3.14 能否安裝 `zxing-cpp` → 可以（zxing-cpp 3.1.1）
- [x] 卡面有無刮刮碼 → 無；但下方條碼含未印出的 8 碼隱藏碼
- [x] 履約保證期間 → 不記錄
- [x] 螢幕亮度 → 網頁無法控制，維持 PWA + 提示 + Wake Lock
- [x] 本機資料加密 → 不加密，明文 + 應用鎖（§6.2）
- [x] 部署 → GitHub Pages（§3.1）
- [x] 卡片組成 → 以 35 / 50 元小卡為主；maxCards 預設 6；預設策略 B 且不多付現金

---

## 13. 修訂紀錄

### v1.3（2026-09-14）

| 項目 | v1.2 | v1.3 | 理由 |
| --- | --- | --- | --- |
| T-03 | 開發前必須門市實刷 | 暫緩，M2 部署後以 App 出示頁做 FT-01 | 使用者決策：先有可用 App 再一次測試；用實際出示頁測比用 PNG 更貼近真實 |
| 部署網址 | `<帳號>.github.io/<repo>/` | `https://seven777-a.github.io/7cw/`（repo `SEven777-a/7cw`） | 網址自此固定 |
| 測試手機 | 未定 | iPhone（主路徑為 polyfill 掃描） | 使用者回答 |
| §1 範例卡號 | 實卡號 | 遮罩 | 本文件會進公開 repo |
| 策略 A 扣款分配 | 「剛好夠的卡」為部分使用 | 選卡規則不變，扣款一律以 `allocate` 計算（由小到大扣，最大那張部分使用） | §8.6 要求輸出能以 allocate 重算；兩種分配剩餘總額與清空張數相同 |
| M1 完成標準 | §10.2 全綠 | DT-04～06、DT-10 移到 M4 | 這幾項測的是匯出匯入，功能在 M4 |
| Settings | — | 加 `hideBrightnessHint` | FR-04「不再顯示」需要存放處 |

### v1.2（2026-09-13）— 依實卡照片 T-01 解碼結果

| 項目 | v1.1 | v1.2 | 理由 |
| --- | --- | --- | --- |
| 條碼數量 | 假設一條 | **兩條 Code 128** | 實卡照片解出上方 16 碼卡號、下方 8 碼英數隱藏碼 |
| 資料模型 | 只有 `code` | 加 `hiddenCode`（唯一索引、格式驗證） | 隱藏碼推定為真正的驗證憑證 |
| FR-01 新增 | 掃描失敗可手動輸入 16 碼 | 必須取得兩條；失敗改照片離線解碼；不得只存卡號 | 隱藏碼沒印在卡面，無法人工抄寫 |
| FR-04 出示 | 單條條碼 | 兩條上下排列、同一畫面、版面集中於 `BARCODE_LAYOUT` | 仿實體卡；店員掃法待 T-03 確認 |
| QR | 待確認 | 忽略、不引入 `qrcode` 套件 | T-02：靜態介紹網址 |
| §1 卡面事實 | 引文不完整 | 補上補發條款、紙卡、發行日與履約保證期間 | 依實卡全文 |
| 實體卡 | 未提 | 明確要求保留 | 可辨識的毀損卡能付費補發 |
| T-01 / T-03 | 文件內嵌範例程式 | 改為 `tools/t01_detect.py`、`tools/t03_redraw.py` | 已實際執行並回讀驗證 |
| 版控 | 規定不得進版控 | 已建立 `.gitignore`（`samples/`、圖片檔） | 實卡照片與重繪圖等同現金 |

### v1.1（2026-09-13）

| 項目 | v1.0 | v1.1 | 理由 |
| --- | --- | --- | --- |
| 螢幕亮度 | 自動拉到最大 | Wake Lock + 提示手動調亮 | iOS / Android 網頁皆無亮度 API；不為此改原生 App |
| 掃描 | BarcodeDetector 為主，`@zxing/library` 備援 | 原生優先，`barcode-detector` polyfill（本地 wasm） | iOS 無原生 BarcodeDetector；`@zxing/library` 僅剩維護；wasm 預設走 CDN 違反 NFR-01 |
| 部署 | 未規定 | GitHub Pages、網址固定、真實卡資料不進版控 | PWA 安裝與相機皆需 HTTPS；換 origin 等於資料全失 |
| Standalone | 引導加入主畫面 | 非 standalone 停用寫入 | iOS Safari 分頁與主畫面 App 儲存分離 |
| 策略 B | 純貪婪 | 貪婪與策略 A 比較後取優 | 純貪婪可能多付現金（UT-13）；單張剛好時多刷一張（UT-09） |
| UT-09 | 策略 B：30 + 50 扣 20 | 兩策略皆只刷 50 | 剩餘零頭張數相同，少刷一張 |
| maxCards 預設 | 3 | 6 | 以 35 / 50 元小卡為主，3 張約 150 元即需補現金 |
| 手動調整 | 未規定分配規則 | §8.6 allocate | FR-03 需要可重算的明確規則 |
| 回填中斷 | 未確認視為未發生 | pending 交易，重開強制回填 | 卡已在現實中被扣款，視為未發生會讓下次建議用到沒錢的卡 |
| NFR-03 | 冷啟動 3 秒到出示頁 | 拆成三段、不含使用者操作 | 與應用鎖、輸入金額衝突，原規格無法量測 |
| T-01 | 裁切到條碼區 | 整張照片、加印 symbology identifier | 裁切後解不到 QR；需辨識 GS1-128 |
| T-03 | 未規定重繪方式 | Python 重繪 + 回讀驗證 | M0 不必寫 App |
| 照片 | base64（§3）與 Blob（§4）矛盾 | Blob，壓縮長邊 1600px | 省空間，規格統一 |
| PBKDF2 | ≥ 100k | 600k，並規定檔案格式 | 對齊 OWASP 現行建議；匯入需知道參數 |
| 匯出/匯入 | 未規定 iOS 行為 | `navigator.share`、input 不限 accept | iOS 主畫面模式下 download 與副檔名過濾不可靠 |
| 資安 | 「最高優先級」未界定 | §6.2 威脅模型：明文 + 應用鎖 | 短 PIN 衍生金鑰可被暴力破解，本機加密實益低、遺失風險高 |
| 資料模型 | — | Transaction 加 status/order/swiped；Settings 加 PIN、備份時間等欄位 | 支撐 pending 流程、應用鎖、備份提醒 |
